# Add lsf-outbox-postgres-starter and a PostgreSQL driver before using this profile.
lsf:
  outbox:
    enabled: true
    table: lsf_outbox
    publisher:
      enabled: true
      scheduling-enabled: true
      batch-size: 50
      claim-strategy: LEASE
